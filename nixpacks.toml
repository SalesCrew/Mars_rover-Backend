[phases.setup]
aptPkgs = ["python3", "python3-pip"]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["npm ci", "python3 --version", "python3 -m pip --version", "python3 -m pip install -r src/exporters/requirements.txt"]

[start]
cmd = "npm start"
